/**
 * Compatibility import path. The canonical document model lives with the
 * renderer so every paper surface shares one immutable presentation contract.
 */
export * from '../../../packages/kubri-document-renderer/src/documentViewModel'
